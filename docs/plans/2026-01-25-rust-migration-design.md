# Project Plan: OpenCode Vibe Kanban - Rust Migration

*Created: 2026-01-25*
*Status: Approved*

## 1. 项目目标

追赶 [BloopAI Vibe Kanban](https://github.com/BloopAI/vibe-kanban)，将后端从 TypeScript 迁移到 Rust，实现专业级 AI Agent 编排平台。

### 核心痛点 (待解决)

| 痛点 | 目标 |
|------|------|
| 任务创建体验差 | 完整表单 UI + Agent Profile 选择 |
| 任务执行监控差 | 实时日志 + 结构化事件 + 中途干预 |
| 缺少代码审查 | Diff 视图 + 行级评论 + 反馈循环 |
| 缺少任务隔离 | Git Worktree 隔离执行 |

---

## 2. 目标架构 (修正版 v2)

```
/
├── crates/                    # Rust 后端 (编排层)
│   ├── core/                  # 核心业务逻辑
│   ├── agent-runner/          # Agent 调度器 (调用 Worker)
│   ├── git-worktree/          # Git Worktree 管理
│   └── api-server/            # 主 API 服务 (Rust)
├── services/                  # 微服务层
│   └── agent-worker/          # Agent 执行服务 (Node.js + OpenCode SDK)
├── frontend/                  # React 前端
└── packages/                  # 共享库
```

### 核心变更: Agent Worker 模式
为解决跨语言调用 CLI 的不稳定性，我们将 Agent 执行逻辑剥离为独立的 Node.js 服务。

*   **Rust (Manager)**: 负责任务状态管理、Git 环境准备、WebSocket 消息推送。
*   **Node.js (Worker)**: 负责加载 OpenCode SDK，执行具体任务，通过 HTTP/Stream 返回结果。

---

## 5. 阶段 2: 任务隔离执行 (重构)

### M-2.1: Git Worktree 核心模块 (Rust)
*(已完成)*

### M-2.2: Agent Worker 服务 (Node.js)
**目标**: 创建一个长驻的 Node.js 服务，封装 OpenCode SDK。

**产出物**:
```
/services/agent-worker/
├── src/
│   ├── server.ts           # Fastify/Express 服务器
│   ├── agent.ts            # OpenCode SDK 包装器
│   └── types.ts
```

**API 定义**:
- `POST /execute`: 接收任务上下文 (cwd, prompt)，启动 Agent。
- `POST /stop`: 停止 Agent。
- **SSE (Server-Sent Events)**: 流式返回思考、工具调用和日志。

### M-2.3: 任务执行器适配 (Rust)
**目标**: 修改 `agent-runner`，使其作为 HTTP 客户端调用 Worker。

**变更**:
- 废弃 `process.rs` (Spawn CLI 逻辑)。
- 新增 `client.rs`: 调用 `http://localhost:port/execute`。
- 转发 SSE 事件流到内部 Event Bus。

### M-2.4: 前端任务执行集成
*(已完成，无需变动，接口保持一致)*

---

## 6. 阶段 3: 任务执行监控

### M-3.1: 结构化事件流 (Node -> Rust)
**目标**: 确保 Worker 返回结构化的事件，而非原始文本。

**事件协议**:
Worker 直接返回 JSON 对象流，Rust 仅需透传或简单转换，不再需要复杂的正则解析。

### M-3.2: 实时事件推送
*(进行中)* Rust 将从 Worker 收到的事件推送到前端。

### M-3.3: 前端实时日志 UI
*(待开始)*

---

---

### M-3.3: 前端实时日志 UI

**前置任务**: M-3.2

**目标**: 展示结构化执行日志

**产出物**:
```
/packages/client/src/components/
├── execution/
│   ├── ExecutionPanel.tsx      # 执行面板
│   ├── EventList.tsx           # 事件列表
│   ├── ThinkingEvent.tsx       # 思考事件
│   ├── CommandEvent.tsx        # 命令事件
│   ├── FileChangeEvent.tsx     # 文件变更事件
│   └── MessageInput.tsx        # 追加消息输入
```

**UI 设计**:
```
┌─────────────────────────────────────────┐
│ Task: 实现用户登录功能                    │
│ Status: Running  [Stop]                 │
├─────────────────────────────────────────┤
│ 🤔 Thinking: 分析需求...                 │
│ ▶ Command: npm install bcrypt           │
│   └─ Output: added 5 packages           │
│ 📝 File: src/auth/login.ts              │
│   └─ +15 lines, -3 lines  [Expand]      │
│ 💬 Message: 我已完成登录接口...          │
├─────────────────────────────────────────┤
│ [输入追加指令...]                [Send]  │
└─────────────────────────────────────────┘
```

**验收标准**:
- 任务执行时实时显示事件
- 可以展开文件变更查看 diff
- 可以发送追加消息给 Agent

**估计工作量**: 大

---

### M-3.4: 操作审批流 (可选)

**前置任务**: M-3.2

**目标**: 危险操作需用户确认

**产出物**:
```
/crates/agent-runner/src/
├── approval.rs             # 审批逻辑

/packages/client/src/components/execution/
├── ApprovalPrompt.tsx      # 审批弹窗
```

**危险操作定义**:
- 删除文件
- 执行 `rm`, `git reset --hard` 等命令
- 修改敏感配置文件

**验收标准**:
- Agent 执行危险操作时暂停
- UI 显示审批请求
- 用户点击"允许"或"拒绝"
- Agent 根据决策继续或回滚

**估计工作量**: 中

---

## 7. 阶段 4: 代码审查

### M-4.1: Git Diff 提取 (Rust)

**前置任务**: M-2.1

**目标**: 提取任务产生的代码变更

**产出物**:
```
/crates/git-worktree/src/
├── diff.rs                 # Diff 提取
```

**核心功能**:
```rust
pub struct DiffExtractor { ... }

impl DiffExtractor {
    /// 获取相对于 base branch 的所有变更
    pub fn get_changes(&self, worktree_path: &Path) -> Result<Vec<FileChange>>;
    
    /// 获取单个文件的详细 diff
    pub fn get_file_diff(&self, path: &Path) -> Result<FileDiff>;
}

pub struct FileChange {
    pub path: String,
    pub status: ChangeStatus,  // Added, Modified, Deleted
    pub additions: u32,
    pub deletions: u32,
}

pub struct FileDiff {
    pub path: String,
    pub hunks: Vec<DiffHunk>,
}
```

**验收标准**:
```bash
cargo test -p git-worktree
# 测试用例:
# - 获取新增文件
# - 获取修改文件的 diff
# - 获取删除文件
# - 统计行数变化
```

**估计工作量**: 中

---

### M-4.2: 代码审查 API (Rust)

**前置任务**: M-4.1

**目标**: 暴露代码审查相关 API

**产出物**:
```
/crates/api-server/src/routes/
├── review.rs               # /api/tasks/:id/changes 路由
```

**API 定义**:
| Method | Path | 说明 |
|--------|------|------|
| GET | /api/tasks/:id/changes | 获取变更文件列表 |
| GET | /api/tasks/:id/changes/:path | 获取单文件 diff |
| POST | /api/tasks/:id/comments | 添加行级评论 |
| GET | /api/tasks/:id/comments | 获取所有评论 |
| POST | /api/tasks/:id/feedback | 将评论发送给 Agent |

**验收标准**:
```bash
# 获取变更列表
curl http://localhost:3001/api/tasks/123/changes
# 返回 [{ "path": "src/auth.ts", "status": "modified", "additions": 10, "deletions": 2 }]

# 获取文件 diff
curl http://localhost:3001/api/tasks/123/changes/src%2Fauth.ts
# 返回详细的 hunk 信息
```

**估计工作量**: 中

---

### M-4.3: 前端 Diff 视图组件

**前置任务**: M-4.2

**目标**: 实现类似 GitHub 的 Diff 视图

**产出物**:
```
/packages/client/src/components/
├── review/
│   ├── ChangesPanel.tsx        # 变更列表面板
│   ├── FileTree.tsx            # 文件树
│   ├── DiffViewer.tsx          # Diff 视图
│   ├── DiffHunk.tsx            # 单个 Hunk
│   ├── LineComment.tsx         # 行级评论
│   └── CommentInput.tsx        # 评论输入框
```

**UI 设计**:
```
┌──────────────────────────────────────────────────────┐
│ Changes (3 files)                                    │
├──────────────────────────────────────────────────────┤
│ 📁 src/                                              │
│   ├─ ✏️ auth.ts (+10, -2)                            │
│   ├─ ➕ login.ts (+45)                               │
│   └─ ➕ register.ts (+38)                            │
├──────────────────────────────────────────────────────┤
│ src/auth.ts                                          │
│ ─────────────────────────────────────────────────── │
│  10  │ - const secret = "old";                       │
│  10  │ + const secret = process.env.JWT_SECRET;      │
│      │   💬 [Add comment]                            │
│  11  │   export const verify = (token) => { ...      │
├──────────────────────────────────────────────────────┤
│ [Send feedback to Agent]                             │
└──────────────────────────────────────────────────────┘
```

**验收标准**:
- 显示变更文件树
- 点击文件显示 diff
- 可以在任意行添加评论
- 可以一键将所有评论发送给 Agent

**估计工作量**: 大

---

### M-4.4: 反馈循环

**前置任务**: M-4.3, M-3.2

**目标**: 将评论反馈给 Agent

**产出物**:
```
/crates/agent-runner/src/
├── feedback.rs             # 反馈处理
```

**流程**:
```
1. 用户在 Diff 视图添加评论
2. 点击 "Send feedback"
3. 评论格式化为 Agent 可理解的消息:
   "请修改以下问题:
   - src/auth.ts:10 - 请使用环境变量而不是硬编码
   - src/login.ts:25 - 这里需要添加错误处理"
4. 消息发送给 Agent
5. Agent 继续执行修改
```

**验收标准**:
- 评论发送后 Agent 收到并响应
- Agent 修改后 Diff 视图更新

**估计工作量**: 中

---

## 8. 任务总览

| 阶段 | 任务 ID | 任务标题 | 前置任务 | 工作量 |
|------|---------|----------|----------|--------|
| 0 | M-0.1 | Rust 项目骨架 | - | 小 |
| 0 | M-0.2 | 开发环境配置 | M-0.1 | 小 |
| 0 | M-0.3 | API 契约定义 | M-0.1 | 中 |
| 1 | M-1.1 | 任务存储层 (Rust) | M-0.1, M-0.3 | 中 |
| 1 | M-1.2 | 任务 API 端点 (Rust) | M-1.1 | 中 |
| 1 | M-1.3 | 前端任务创建 Modal | M-1.2 | 中 |
| 1 | M-1.4 | 前端迁移到 Rust API | M-1.3 | 小 |
| 2 | M-2.1 | Git Worktree 核心模块 | M-0.1 | 中 |
| 2 | M-2.2 | 任务执行器 (Rust) | M-2.1, M-1.1 | 大 |
| 2 | M-2.3 | 执行器 API 端点 | M-2.2 | 中 |
| 2 | M-2.4 | 前端任务执行集成 | M-2.3, M-1.3 | 中 |
| 3 | M-3.1 | 输出解析器 | M-2.2 | 中 |
| 3 | M-3.2 | 实时事件推送 (WebSocket) | M-3.1 | 中 |
| 3 | M-3.3 | 前端实时日志 UI | M-3.2 | 大 |
| 3 | M-3.4 | 操作审批流 (可选) | M-3.2 | 中 |
| 4 | M-4.1 | Git Diff 提取 | M-2.1 | 中 |
| 4 | M-4.2 | 代码审查 API | M-4.1 | 中 |
| 4 | M-4.3 | 前端 Diff 视图组件 | M-4.2 | 大 |
| 4 | M-4.4 | 反馈循环 | M-4.3, M-3.2 | 中 |

---

## 9. 依赖关系图

```
M-0.1 ─────┬─────────────────────────────┐
           │                             │
           ▼                             ▼
        M-0.2                         M-0.3
                                        │
           ┌────────────────────────────┤
           │                            │
           ▼                            ▼
        M-2.1                        M-1.1
           │                            │
           ├──────────────┐             ▼
           │              │          M-1.2
           ▼              │             │
        M-4.1             │             ▼
           │              │          M-1.3
           ▼              │             │
        M-4.2             ▼             ▼
           │           M-2.2 ←───── M-1.4
           ▼              │
        M-4.3             ├──────────────┐
           │              │              │
           │              ▼              ▼
           │           M-2.3          M-3.1
           │              │              │
           │              ▼              ▼
           │           M-2.4          M-3.2
           │                             │
           │              ┌──────────────┼──────────────┐
           │              │              │              │
           │              ▼              ▼              ▼
           │           M-3.3          M-3.4          M-4.4
           │                                            │
           └────────────────────────────────────────────┘
```

---

## 10. 推荐启动顺序

**第一周**: M-0.1 → M-0.2 → M-0.3 (基础设施)

**第二周**: M-1.1 → M-1.2 (任务存储)

**第三周**: M-1.3 → M-1.4 + M-2.1 (并行: 前端表单 + Git Worktree)

---

*Created by OpenCode Brainstorming Skill*
